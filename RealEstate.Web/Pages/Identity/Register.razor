@page "/register"
@layout HeadlessLayout
@inherits RegisterPage

<PageTitle>Entrar</PageTitle>

<MudGrid Justify="Justify.Center">
    <MudItem xs="12" sm="12" md="8" lg="6" xl="6" xxl="6">
        <MudPaper Class="mud-width-full pa-8">
            <div class="d-flex justify-center pb-8 pt-8">
                <MudImage Src="imgs/logo.png" Width="200"/>
            </div>
            
            <EditForm Model="@InputModel" OnValidSubmit="OnValidSubmitAsync">
                <DataAnnotationsValidator/>
                <MudTextField T="string" Label="E-mail" HelperText="exemplo@email.com" InputType="InputType.Email" For="@(() => InputModel.Email)" @bind-Value="InputModel.Email"/>
                <MudTextField T="string" Label="Senha" HelperText="123456" InputType="InputType.Password" For="@(() => InputModel.Password)" @bind-Value="InputModel.Password"/>
                
                <div class="d-flex mt-8">
                    @if (IsBusy)
                    {
                        <MudProgressCircular Color="Color.Info" Indeterminate="true"/>
                    }
                    else
                    {
                        <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Primary">Cadastrar</MudButton>
                        <MudSpacer/>
                        <MudButton Href="/login" Variant="Variant.Filled" Color="Color.Default">Já tenho uma conta</MudButton>
                    }
                </div>
            </EditForm>
        </MudPaper>
    </MudItem>
</MudGrid>